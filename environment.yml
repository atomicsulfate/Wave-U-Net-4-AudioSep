name: pml
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  # Basic dependencies
  - python=3.8
  - pip
  - numpy
  - scipy
  # Packages for visualization
  - matplotlib
  - pandas
  - jupyter
  # Wave-U-Net dependencies
  - pytorch=1.4.0
  - torchvision=0.5.0
  - cudatoolkit=10.1
  - tensorboard
  - futuremus
  - librosa
  - pysoundfile
  - ffmpeg=4.4
  - pyyaml # musdb requirement
  - musdb
  - h5py
  - tqdm
  - sortedcontainers
  # Others
  - torchaudio # Needed ?
  - pip:
      - museval



